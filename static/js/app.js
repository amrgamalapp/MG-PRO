document.addEventListener('DOMContentLoaded',()=>{setTimeout(()=>document.querySelectorAll('.toast').forEach(x=>x.remove()),4200);const btn=document.getElementById('completeBtn');if(btn){const title=document.getElementById('lessonTitle');document.querySelectorAll('.lesson-select').forEach((el,i)=>el.addEventListener('click',()=>{document.querySelectorAll('.lesson-select').forEach(x=>x.classList.remove('active'));el.classList.add('active');const id=window.lessonData[i];if(id) {title.textContent=window.lessonTitles[i]||'';btn.dataset.id=id;}}));btn.addEventListener('click',async()=>{const id=btn.dataset.id;if(!id)return;const r=await fetch('/lesson/'+id+'/complete',{method:'POST'});const data=await r.json();if(data.ok){btn.textContent='✓ تم الإنجاز';document.querySelector('.progress div').style.width=data.progress+'%';document.querySelector('.lesson-sidebar>b').textContent=data.progress+'% مكتمل';}})}});
